{
  "name": "gobarber",
  "private": true,
  "version": "1.0.0",
  "license": "MIT",
  "scripts": {
    "test": "jest",
    "lint": "yarn workspaces run lint",
    "dev:backend": "yarn workspace @gobarber/backend dev"
  },
  "workspaces": {
    "packages": [
      "packages/*"
    ],
    "nohoist": [
      "*react-native*",
      "*react-native*/**"
    ]
  },
  "dependencies": {},
  "devDependencies": {
    "@gobarber/eslint-config": "*",
    "@types/jest": "^26.0.5",
    "jest": "^26.1.0",
    "ts-jest": "^26.1.3",
    "typescript": "^3.9.7"
  }
}
